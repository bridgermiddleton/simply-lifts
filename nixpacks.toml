[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.build]
cmds = ["npm install", "npm run build", "pip install -r server/requirements.txt"]

[start]
cmd = "echo 'Starting Flask...' && python3 server/app.py"